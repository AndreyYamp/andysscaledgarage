languageCode: en-us
title: Andrey's Scaled Garage

module:
  hugoVersion:
    min: "0.146.0"
    extended: true
  mounts:
  - source: content
    target: content
  - source: static
    target: static
  - source: layouts
    target: layouts
  - source: data
    target: data
  - source: assets
    target: assets
  - source: i18n
    target: i18n
  - source: archetypes
    target: archetypes
  - source: ../../node_modules/flag-icons/flags
    target: static/flags
  - source: ../../node_modules/@fontsource/mulish/files
    target: static/files
  - source: ../../node_modules/katex/dist/fonts
    target: static/fonts

markup:
  goldmark:
    renderer:
      unsafe: true

params:
  description: A calm project journal for scale model car builds.
  features:
    homeJournal:
      enable: true
      sectionLabel: Featured Projects
      archiveLabel: Browse the full project archive
      featuredLabel: Featured Project
